# Purpose:      Adapters sub-package for the ingestion pipeline.
#               Each adapter knows how to fetch raw events from one source type.
#               SyntheticAdapter is the only adapter in Phase 3A.
